_schema-version: 3.3.0
ID: Factura
description: A simple CAP project.
version: 1.0.1

modules:
- name: Factura-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: Factura-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci

- name: Factura-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: Factura-db
  parameters:
    buildpack: nodejs_buildpack

- name: Factura-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: Factura-html5-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - qfacturas.zip
      name: qfacturas
      target-path: resources/

- name: qfacturas
  type: html5
  path: app/qfacturas
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []

resources:
- name: Factura-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared

- name: Factura-html5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host

- name: Factura-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
    service: destination
    service-plan: lite
  requires:
    - name: Factura-html5-repo-host

parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true

build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production